---
import ValidationFields from './ValidationFields.astro';
---

<form id="emergency-form" class="space-y-5 border border-red-700 bg-white p-6">
  <input type="hidden" name="intakeType" value="emergency" />
  <ValidationFields />

  <label class="block">
    <span class="font-bold">Name</span>
    <input required name="name" class="mt-2 w-full border border-[var(--color-line)] p-3" />
  </label>

  <label class="block">
    <span class="font-bold">Email</span>
    <input required type="email" name="email" class="mt-2 w-full border border-[var(--color-line)] p-3" />
  </label>

  <label class="block">
    <span class="font-bold">SLA / Site Reference</span>
    <input required name="contractReference" class="mt-2 w-full border border-[var(--color-line)] p-3" />
  </label>

  <label class="block">
    <span class="font-bold">Failure Impact</span>
    <textarea required name="message" rows="6" class="mt-2 w-full border border-[var(--color-line)] p-3"></textarea>
  </label>

  <div id="emergency-form-error" style="display:none" class="border border-red-300 bg-red-50 p-4 text-sm font-bold text-red-700">
    Submission failed. Please check your details and try again, or contact us directly.
  </div>

  <button id="emergency-submit" class="bg-red-700 px-5 py-3 font-bold text-white">Submit Emergency Intake</button>
</form>

<script>
  const form = document.getElementById('emergency-form') as HTMLFormElement | null;
  const errEl = document.getElementById('emergency-form-error') as HTMLElement | null;
  const btn = document.getElementById('emergency-submit') as HTMLButtonElement | null;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const data = new FormData(form!);
    if (data.get('company')) return;

    if (btn) { btn.disabled = true; btn.textContent = 'Submitting…'; }
    if (errEl) errEl.style.display = 'none';

    try {
      const res = await fetch('/api/triage', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          type: data.get('intakeType'),
          name: data.get('name'),
          email: data.get('email'),
          message: data.get('message'),
          priority: 'critical'
        })
      });
      const json = await res.json();
      if (res.ok && json.success) {
        window.location.href = '/triage/confirmation?type=emergency';
      } else {
        throw new Error('submission failed');
      }
    } catch {
      if (btn) { btn.disabled = false; btn.textContent = 'Submit Emergency Intake'; }
      if (errEl) errEl.style.display = 'block';
    }
  });
</script>
